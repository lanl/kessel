spack:
  extends: custom
  view:
    default:
      root: .spack-env/view
      exclude: [intel-oneapi-runtime, gcc-runtime]
  packages:
    c:
      prefer:
       - oneapi
    cxx:
      prefer:
       - oneapi
    fortran:
      prefer:
       - oneapi
