spack:
  extends: custom
  view:
    default:
      root: .spack-env/view
      exclude: ['gcc-runtime']
  packages:
    c:
      prefer:
       - cce
    cxx:
      prefer:
       - cce
    fortran:
      prefer:
       - cce
