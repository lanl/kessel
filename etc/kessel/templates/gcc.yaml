spack:
  extends: custom
  view:
    default:
      root: .spack-env/view
      exclude: ['gcc-runtime']
  packages:
    c:
      prefer:
       - gcc
    cxx:
      prefer:
       - gcc
    fortran:
      prefer:
       - gcc
