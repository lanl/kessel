spack:
  extends: custom
  view:
    default:
      root: .spack-env/view
      exclude: ['gcc-runtime']
  packages:
    c:
      prefer:
       - llvm
    cxx:
      prefer:
       - llvm
    fortran:
      prefer:
       - gcc
