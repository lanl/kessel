spack:
  extends: custom
  view:
    default:
      root: .spack-env/view
      exclude: ['gcc-runtime']
  packages:
    c:
      prefer:
       - clang
    cxx:
      prefer:
       - clang
    fortran:
      prefer:
       - gcc
