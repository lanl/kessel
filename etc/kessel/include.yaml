include:
  - when: env['KESSEL_DEPLOYMENT'] != env['KESSEL_PARENT_DEPLOYMENT']
    path: parent.yaml
  - when: env.get("KESSEL_REQUIRE_SYSTEM_MIRROR", "none") == "pe-serve"
    path: lanl_pe_serve_mirror.yaml
