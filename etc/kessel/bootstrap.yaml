bootstrap:
  sources:
  - name: local-binaries
    metadata: ${KESSEL_DEPLOYMENT}/spack-bootstrap/metadata/binaries
  - name: local-source
    metadata: ${KESSEL_DEPLOYMENT}/spack-bootstrap/metadata/sources
  trusted:
    local-source: true
    local-binaries: true
