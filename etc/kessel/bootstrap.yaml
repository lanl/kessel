bootstrap:
  sources:
  - name: local-binaries
    metadata: ${KESSEL_PARENT_DEPLOYMENT}/spack-bootstrap/metadata/binaries
  - name: local-source
    metadata: ${KESSEL_PARENT_DEPLOYMENT}/spack-bootstrap/metadata/sources
  trusted:
    local-source: true
    local-binaries: true
